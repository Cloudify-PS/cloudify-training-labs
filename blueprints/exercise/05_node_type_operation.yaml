tosca_definitions_version: cloudify_dsl_1_3

imports:
  - http://www.getcloudify.org/spec/cloudify/3.4.1/types.yaml

node_types:
  apache:
    derived_from: cloudify.nodes.Root
    properties:
      listener_port:
        type: integer
        default: 80
    # Add the "create" operation in the "cloudify.interfaces.lifecycle" interface.
    # The operation should be mapped to the implementation "scripts/apache-install.sh".
    # Also, add a "delete" operation in that same interface, mapped to the implementation
    # "scripts/apache-uninstall.sh".
